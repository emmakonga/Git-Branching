using UnityEngine;
using System.Collections;

public class MyScript3 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		// I am given a whole pizza
	}
	
	// Update is called once per frame
	void Update () {
		// I eat a slice of pizza every frame
	}
}
